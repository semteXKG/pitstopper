<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <!-- Throttle bar -->
    <TextView
        android:id="@+id/slotThrottleLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="THR"
        android:textColor="@color/telemetry_label"
        android:textSize="28sp" />

    <FrameLayout
        android:id="@+id/throttleBarContainer"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_marginTop="2dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:background="#333333">

        <View
            android:id="@+id/throttleBarFill"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@color/throttle_bar" />
    </FrameLayout>

    <TextView
        android:id="@+id/slotThrottleValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="-- %"
        android:textColor="@color/text_primary"
        android:textSize="36sp" />

    <!-- Brake indicator -->
    <TextView
        android:id="@+id/slotBrakeLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:fontFamily="monospace"
        android:text="BRK"
        android:textColor="@color/telemetry_label"
        android:textSize="28sp" />

    <View
        android:id="@+id/brakeIndicator"
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:layout_marginTop="2dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:background="#333333" />

</LinearLayout>
