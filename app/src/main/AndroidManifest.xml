<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- GPS location permission for detecting standstill in pit stops -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Internet permission for SpeedHive Live Timing API and MQTT -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- WiFi permissions for network binding and SSID scan -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Microphone permission for push-to-talk speech recognition -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".activities.PitStopperApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pitstopper">

        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link: pitstopper://join?session={uuid} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pitstopper" android:host="join" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SettingsRaceActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SettingsSpeedHiveActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SettingsTelemetryActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SettingsMqttActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SessionActivity"
            android:exported="false" />

        <activity
            android:name=".activities.FlashMessageActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <activity
            android:name=".activities.ChatMessageActivity"
            android:exported="false"
            android:launchMode="singleTop" />

    </application>

</manifest>
